base_path = "."
error_log = f"{base_path}/log/gitporter_error.log"
# path to log file
log_file = f"{base_path}/log/gitporter.log"
# max size of log_file in KB, if this limitation is exceeded, the script will generate a new logfile
log_size = 200